/**
 * @author liuchuanyang
 * @date 2017-03-13
 */

import $ from 'jquery'
import Utils from '../assets/js/utils/utils'

export default {
    inserted (el, binding, vnode) {

        let $el = $(el);
        
        $el.on('click', '.tab label', )
    }
}
